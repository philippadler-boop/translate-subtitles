srt
tqdm
python-dotenv

deep-translator      # Google (and optional other web-based engines)
deepl                # Official DeepL API SDK
openai               # GPT-4o
transformers         # HuggingFace models
torch                # backend for transformers (CPU is fine)
sentencepiece        # Required for some HuggingFace tokenizers (e.g., opus-mt)
sacremoses           # Additional tokenizer support
faster-whisper       # Fast local Whisper implementation for ASR
ffmpeg-python        # Python bindings for ffmpeg (audio extraction helper)
webrtcvad            # Voice Activity Detection for segmentation

# Testing dependencies
pytest               # Test framework
pytest-cov           # Coverage reporting
